import { ElementResizeDirective } from './element-resize.directive';

describe('ElementResizeDirective', () => {
  it('should create an instance', () => {
    const directive = new ElementResizeDirective();
    expect(directive).toBeTruthy();
  });
});
